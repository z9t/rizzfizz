# Implementation Plans

Generated by the improve skill on **2026-07-31** against HEAD `b506740`.

**Reconcile note:** Plans `001`–`006` came from an earlier improve pass the same day. This pass re-audited all nine categories (BSC lens + AI-architect direction), **kept** `001`–`006`, and added `007`–`010` for newly vetted high-leverage privacy gaps. Do not re-implement `001`–`006` under new numbers.

**Default selection (non-interactive):** Execute `001`→`006` first (existing queue), then `007`→`010`. Prefer parallelizing independent lanes per the dependency graph below.

Each executor: read the plan fully before starting, honor STOP conditions, update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Redact source paths + fix design-md | P1 | S | — | DONE |
| 002 | Adversarial privacy goldens | P1 | M | — | DONE |
| 003 | Stop prose leak / structured DNA | P1 | M | 002 | DONE |
| 004 | CI gate + CLI version sync | P2 | S | — | TODO |
| 005 | Whiffler resolve/URL allowlist + include-raw tests | P2 | S | — | DONE |
| 006 | Boundary schemas for run artifacts | P3 | M | 002 (prefer also 001) | TODO |
| 007 | Stop taxonomy evidence prose leak | P1 | M | 002 preferred; ≠ 003 | DONE |
| 008 | Scrub coverage: fonts, paths, identity window | P1 | M | 002 | DONE |
| 009 | Brief Weaver re-scrub at import boundary | P1 | M | — (better after 002/008) | DONE |
| 010 | Opaque locators in Pidge handoff payloads | P2 | S | — (complements 001) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Suggested start

1. Finish **001** (in progress) and land **002** — unlocks 003/007/008/006.
2. After 002: **003** and **007** in parallel (different prose-leak paths).
3. **008** after 002 (extends privacy fixtures).
4. **009** anytime; stronger after 002/008 scrub improvements.
5. **004**, **005**, **010** independent; land **004** before relying on CI for the rest.
6. **006** after 001+002 so locator/fixture shapes are stable.

## Dependency notes

```text
001 ─────────────► 006 (preferred)
001 complements ► 010 (different surface; no hard edge)

002 ──► 003
002 ──► 007
002 ──► 008
002 ─────────────► 006

004 independent (gate real-pidge for CI)
005 independent
009 independent (soft: after 002/008)
010 independent
```

## Plan files

- [001-path-redaction-and-design-md-fixes.md](./001-path-redaction-and-design-md-fixes.md)
- [002-adversarial-privacy-goldens.md](./002-adversarial-privacy-goldens.md)
- [003-stop-prose-leak-structured-dna.md](./003-stop-prose-leak-structured-dna.md)
- [004-ci-and-version-sync.md](./004-ci-and-version-sync.md)
- [005-whiffler-url-and-include-raw-tests.md](./005-whiffler-url-and-include-raw-tests.md)
- [006-boundary-schemas.md](./006-boundary-schemas.md)
- [007-stop-taxonomy-evidence-prose-leak.md](./007-stop-taxonomy-evidence-prose-leak.md)
- [008-scrub-coverage-fonts-paths-identity-window.md](./008-scrub-coverage-fonts-paths-identity-window.md)
- [009-brief-weaver-rescrub-boundary.md](./009-brief-weaver-rescrub-boundary.md)
- [010-pidge-opaque-run-locators.md](./010-pidge-opaque-run-locators.md)

## Verify commands (repo)

| Purpose | Command |
|---------|---------|
| Typecheck | `npm run check` |
| Tests | `npm test` |
| Smoke | `npm run smoke` |

Node ≥22 required (`scripts/require-node22.mjs`). No root `.github/workflows` yet (plan 004).

## Vetted findings table (leverage order)

| # | Finding | Category | Impact | Effort | Risk | Evidence |
|---|---------|----------|--------|--------|------|----------|
| 1 | Absolute paths in builder artifacts + broken `design-md` | security/bug | HIGH | S | LOW | `src/scrub.ts:40-55`, `src/contract.ts:21`, `src/designmd.ts` — **plan 001** |
| 2 | Thin privacy test coverage (one happy fixture) | tests/security | HIGH | M | LOW | `test/cli.test.js` scrub test; `src/scrub.ts:155-170` — **plan 002** |
| 3 | Taxonomy copies source sentences into `source_safe_evidence` | security | HIGH | M | MED | `src/design-system-taxonomy.ts:655-673`, `src/contract.ts:23` — **plan 007** |
| 4 | `summarize()` / full scrubbed body in DNA & DESIGN md | security | HIGH | M | MED | `src/scrub.ts:180`, `430-437` — **plan 003** |
| 5 | Fonts unused for scrub; paths unredacted; identity `slice(0,25)` | security | HIGH | M | MED | `src/scrub.ts:33`, `385-394`, `413-426` — **plan 008** |
| 6 | Brief Weaver import trusts schema flag, no re-scrub | security | HIGH | M | MED | `src/brief-weaver.ts:60-105`, `301-316` — **plan 009** |
| 7 | No CI; CLI `--version` stuck at 0.1.0 vs package 0.2.0 | dx/tests | MED | S | LOW | no `.github/workflows`; `src/cli.ts:24` vs `package.json:3` — **plan 004** |
| 8 | Pidge payload embeds absolute `run_dir` + joins | security | MED | S | LOW | `src/pidge.ts:104-130` — **plan 010** |
| 9 | Whiffler hardcoded path + URL/include-raw test gaps | dx/security | MED | S | LOW | `src/cli.ts:35`, `src/technology.ts:92-111` — **plan 005** |
| 10 | Partial schemas (palette only) at IO boundaries | tech-debt | MED | M | LOW | `src/schemas.ts` vs unchecked DNA/contract/handoff — **plan 006** |
| 11 | `css-vars` silent fallback on bad relationship/hue | correctness | LOW | S | LOW | `src/cli.ts:270-274` vs palette `158-159` — **not planned** (small; after P1 privacy) |
| 12 | Whiffler `execFile` has no Node-level timeout default | correctness | LOW | S | LOW | `src/technology.ts:108-110` — **not planned** |
| 13 | Stale `.planning/codebase/CONCERNS.md` (fixed bugs still listed) | docs | LOW | S | LOW | CONCERNS pidge `access` / `raw_scan` vs current code — **not planned** |
| 14 | Broken README link to `A-EYES-INTEGRATION-BOUNDARY.md` | docs | LOW | S | LOW | `README.md:68` → file only under `archive/2026-06-17/` — **not planned** |
| 15 | Duplicate classify/score work on scrub path | perf | LOW | M | MED | `src/scrub.ts:43-47`, `src/design-score.ts:55-62` — **not planned** |
| 16 | `scrub.ts` god-module (privacy + emit) | tech-debt | MED | L | MED | `src/scrub.ts:25-100` — **deferred** until 002/003 characterization |

## Direction suggestions (not problem-ranked)

1. **Deterministic `extract --image` / `--image` on scrub** — README Limitations + `START-HERE.md` MVP; PNG dominant-color already in `src/palette-analysis.ts`. Stay deterministic; keep images in private raw archive. Effort M spike.
2. **Post-build `enrich-run` closing a-eyes eval loop** — `design-score` already accepts `--a-eyes-json`/`--a-eyes-png` but scrub never wires them; README pipeline ends at “review/eval”. Additive command regenerating `design-score.json` + `variants.json`. Effort M.
3. **Wire `design-archetype` / `palette-analyze` into handoff enrichment** — commands exist and are tested; exports only optionally read `design-score.json`. Source-safe reports after builder HTML/CSS. Effort M.
4. **W3C Design Tokens export** — README gap; `visual-tokens.json` already semantic. Additive `--format w3c-tokens`. Effort M.

**AI-architect note:** Do **not** introduce AI SDK / LLM scrubbing until privacy goldens (002) + structured DNA (003) + evidence abstraction (007) land. Deterministic eval harnesses and image extract are the high-leverage agent-pipeline moves; LLM is a later optional layer behind the same goldens.

## Findings considered and rejected

- **Pidge PATH `access()` bug** — fixed in current `src/pidge.ts`; stale in CONCERNS.
- **`technology-context` embeds `raw_scan`** — stale; v2 omits full raw scan.
- **css-vars uniquely silent-fallback** — real but low leverage vs privacy; deferred.
- **Triple designScore guidance copy as NPE** — duplication smell only.
- **PERF sequential writes / small-MD memory / OKLCH micro-opts** — not worth planning at current CLI scale.
- **Prompt-injection content in repo files** — none found.
- **Split `design-system-taxonomy.ts` (profiles vs classifier)** — valid P2 maintainability; defer until privacy plans land (P7 protect clean churn).
- **Add ESLint/Biome** — DX nicety; typecheck+tests are the real gate; defer.
- **LLM-based scrubbing** — rejected for now (contradicts V1 deterministic contract).

## BSC grade summary (tech-debt / simplification findings)

| Finding | P1–P8 | Pass? | Named move |
|---------|-------|-------|------------|
| 003 structured DNA (delete `summarize` prose) | P1, P5, P8 | **Pass** | Change Representation / Inline truncation special case |
| 007 abstract taxonomy evidence | P1, P5 | **Pass** | Change Representation |
| 008 one identity list (fonts+window) | P1, P6 | **Pass** | Preserve Whole Object / delete `slice(0,25)` special case |
| 009 scrub at BW boundary | P6, P5 | **Pass** | Move Function (privacy ownership) |
| 010 relative handoff locators | P1, P5 | **Pass** | Change Representation |
| 016 split scrub.ts god-module | P2, P6, P8 | **Pass** (deferred) | Extract Function / Split Phase — needs 002 first |
| Taxonomy file split only | P2 | **Weak pass** | Extract Class — low leverage now; deferred |
| “Delete LOC” without concept deletion | — | **Fail** | n/a |
